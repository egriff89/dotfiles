module Env

hidden env: Map<String, String> = read("../.env").text.trimStart().split("\n")
  .filter((line) -> line.contains("="))
  .toMap(
    (line) -> line.substring(0, line.indexOf("=")),
    (line) -> line.substring(line.indexOf("=") + 1, line.length).trim()
  )
